# Obsidian keeps per-person editor state here: window layout, open panes, recent files.
# Vault settings worth sharing can be unignored one file at a time.
.obsidian/

# Editor and OS leftovers
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
